events {
    worker_connections 1;
    event_model select;
    listen 80;   # disallowed directive in events
}

http {
    client_max_body_size 1M;
    error_page 404 /errors/404.html;

    server {
        listen 127.0.0.1:8086;
        root /var/www/html;
    }
}
